import React from 'react';

export const metadata = {
  title: 'Book Your Move | Speedy Van Luxury Service',
  description: 'Professional moving service with premium experience. Book online in minutes.',
};

export default function BookingLuxuryLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}

