import { Metadata } from 'next';

export { metadata } from './metadata';

export default function FacebookMarketplaceDeliveryLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
