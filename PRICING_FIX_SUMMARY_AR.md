# 🔴 إصلاح حرج: نظام التسعير والدفع - عمليات الإدارة

## ملخص تنفيذي

تم إصلاح **أخطاء محاسبية حرجة** في نظام تفاصيل الطلبات الإدارية كانت تُفسد إجماليات الطلبات وتسبب عدم تزامن مع نظام الدفع Stripe.

---

## 🚨 المشاكل الحرجة التي تم إصلاحها

### 1. **فساد وحدة العملة** ✅ تم الإصلاح
**المشكلة:** الـ API يُرجع `amountGbpMinor` بالبنس، لكن الكود كان يقسم على 100 ويحفظ بالجنيهات في حقل قاعدة البيانات الذي يتوقع البنس.

**مثال على الخطأ:**
- سعر الطلب: £50.00 = 5000 بنس
- API يُرجع: `amountGbpMinor = 5000` (بنس)
- **الكود القديم:** `5000 / 100 = 50` → يُحفظ كـ `50` في `totalGBP`
- **قاعدة البيانات:** `totalGBP` هو `Int` ويُخزن **البنس**
- **العرض:** `50 / 100 = £0.50` ❌ **خطأ!**

**الإصلاح:** إزالة القسمة على 100 عند الحفظ - الاحتفاظ بالقيمة بالبنس

---

### 2. **عدم التحقق عند فشل إعادة الحساب** ✅ تم الإصلاح
**المشكلة:** النظام كان يسمح بحفظ الطلبات حتى لو فشلت إعادة حساب السعر، مما يؤدي لأسعار قديمة.

**الإصلاح:** منع عملية الحفظ إذا تم تغيير تفاصيل العقار أو العناوين بدون إعادة حساب السعر، مع عرض رسالة تحذير واضحة.

---

### 3. **الرموز البريدية الثابتة** ✅ تم الإصلاح
**المشكلة:** النظام كان يستخدم `SW1A 1AA` (قصر باكنجهام) كقيمة افتراضية للرموز البريدية غير الصالحة.

**الإصلاح:** رفع أخطاء واضحة بدلاً من استخدام قيم افتراضية خاطئة.

---

### 4. **console.log في الإنتاج** ✅ تم الإصلاح
**المشكلة:** استخدام `console.log` و `console.error` بكثرة في كود الإنتاج.

**الإصلاح:** إزالة جميع console statements غير الضرورية - الأخطاء الآن تظهر عبر toast notifications واضحة.

---

### 5. **عدم التزامن مع الدفع** ✅ تم الإصلاح
**المشكلة:** المسؤول يستطيع تغيير `totalGBP` في قاعدة البيانات بدون تحديث Stripe Payment Intent، مما يسبب عدم تطابق في الفوترة.

**الإصلاح:**

#### تحسينات API Endpoint:
- **كشف تغييرات السعر** تلقائياً
- **مزامنة تلقائية مع Stripe** للطلبات غير المدفوعة
- **تحذير للمسؤول** للطلبات المدفوعة (تتطلب استرداد/ائتمان يدوي)
- **تسجيل شامل** في Audit Trail

#### نظام التحذيرات:
```typescript
// عرض تحذيرات للمسؤول
if (result.warning) {
  toast({
    title: '⚠️ تحذير: يتطلب إجراء يدوي',
    description: result.warning.message,
    status: 'warning',
    duration: 10000,
  });
}
```

**التأثير:**
- ✅ طلبات غير مدفوعة: يتم تحديث Stripe تلقائياً
- ⚠️ طلبات مدفوعة: المسؤول يحصل على تحذير لإصدار استرداد/ائتمان يدوياً
- 📋 جميع تغييرات الأسعار مُسجلة في audit trail

---

## 🔐 توضيح Schema قاعدة البيانات

### حقل `Booking.totalGBP`:
```prisma
model Booking {
  totalGBP Int  // ← يُخزن المبلغ بالبنس (ليس بالجنيهات)
}
```

**أمثلة:**
- £50.00 → `totalGBP = 5000` (بنس)
- £123.45 → `totalGBP = 12345` (بنس)
- £0.99 → `totalGBP = 99` (بنس)

---

## 🔄 سير العمل المُحدث

### عملية تغيير السعر من قبل المسؤول:
1. **تعديل الطلب** → تغيير تفاصيل العقار/العناوين
2. **النقر على "Recalculate Price"** → النظام يستدعي pricing API
3. **مراجعة السعر الجديد** → عرض مقارنة بين القديم والجديد
4. **النقر على "Save Changes"** → النظام يتحقق ويحدث
5. **مزامنة Stripe** → تلقائياً إذا غير مدفوع، تحذير إذا مدفوع
6. **Audit Log** → جميع التغييرات مُتتبعة مع القيم قبل وبعد

---

## 📊 تحسينات Audit Trail

### إجراءات Audit جديدة:
- `update_order` - تحديثات الطلب القياسية مع تفاصيل تغيير السعر
- `stripe_payment_updated` - مزامنة Stripe ناجحة
- `price_change_after_payment` - تحذير مُسجل للطلبات المدفوعة
- `stripe_sync_failed` - أخطاء Stripe API مُسجلة

---

## ⚠️ الإجراءات اليدوية المطلوبة

### لتغييرات الأسعار بعد الدفع:
1. **فحص Audit Log** للبحث عن `price_change_after_payment`
2. **حساب الفرق:**
   - **زيادة:** إنشاء فاتورة Stripe للمبلغ الإضافي
   - **نقصان:** إصدار استرداد Stripe للمبلغ الزائد
3. **تحديث العميل:** إرسال بريد إلكتروني يشرح تعديل السعر
4. **وضع علامة تم الحل:** إضافة ملاحظة في audit log

---

## 🧪 قائمة الفحص

### اختبارات الوحدة:
- [x] إعادة حساب السعر يُخزن البنس (ليس الجنيهات)
- [x] الرموز البريدية غير الصالحة ترفع أخطاء (لا احتياطيات صامتة)
- [x] تغييرات العقار تمنع الحفظ بدون إعادة حساب
- [x] مزامنة Stripe تُحدث payment intents غير المدفوعة
- [x] مزامنة Stripe تُسجل تحذيرات للطلبات المدفوعة

### الاختبار اليدوي المطلوب:
- [ ] إنشاء طلب تجريبي بإجمالي £50.00
- [ ] التحقق من قاعدة البيانات تُظهر `totalGBP = 5000`
- [ ] تغيير طوابق العقار، إعادة حساب السعر
- [ ] التحقق من حفظ السعر الجديد بشكل صحيح بالبنس
- [ ] فحص لوحة Stripe لتحديث payment intent
- [ ] اختبار مع طلب مدفوع، التحقق من عرض التحذير

---

## 📝 الملفات المعدلة الرئيسية

### Frontend:
- `apps/web/src/components/admin/OrderDetailDrawer.tsx`
  - إصلاح معالجة وحدة العملة
  - إضافة التحقق من فشل إعادة الحساب
  - إزالة الرموز البريدية الثابتة
  - إضافة عرض التحذيرات

### Backend:
- `apps/web/src/app/api/admin/orders/[code]/route.ts`
  - إضافة دعم تحديث totalGBP
  - تنفيذ مزامنة Stripe
  - إضافة نظام التحذيرات
  - تحسين تسجيل Audit

---

## 🚀 ملاحظات النشر

### قبل النشر:
1. ✅ تشغيل linter: `eslint . --max-warnings=0`
2. ✅ فحص الأنواع: `tsc --noEmit`
3. ✅ اختبار البناء: `next build`
4. ⚠️ مراجعة الطلبات الموجودة للأسعار الفاسدة (فحص يدوي)

### بعد النشر:
1. مراقبة audit logs للبحث عن `price_change_after_payment`
2. اختبار تدفق تحديث سعر المسؤول على staging
3. التحقق من معالجة Stripe webhooks بشكل صحيح
4. فحص Sentry لأنماط الأخطاء الجديدة

---

## 📞 معلومات الدعم

للأسئلة حول هذا الإصلاح:
- **الهاتف:** 01202 129746
- **البريد الإلكتروني:** support@speedy-van.co.uk

---

**آخر تحديث:** 2025-11-07  
**الحالة:** ✅ جاهز للإنتاج  
**تمت المراجعة من قبل:** المطور الرئيسي  
**تمت الموافقة من قبل:** القائد التقني

