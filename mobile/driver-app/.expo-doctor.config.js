module.exports = {
  // Ignore duplicate dependencies check in monorepo
  ignore: ['duplicate-dependencies'],
  // Skip specific checks that are not relevant for this project
  skip: [
    'duplicate-dependencies'
  ]
};
