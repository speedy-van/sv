# 🚨 خطوات حاسمة: عزل قاعدة البيانات فوراً

## الوضع الحالي

تم اكتشاف أن بيئة التطوير متصلة بقاعدة بيانات الإنتاج. هذا **خطير جداً** ويجب إصلاحه فوراً.

---

## ✅ تم إنجازه

1. ✅ إنشاء سكريبتات الإعداد الآلي
2. ✅ إنشاء سكريبت التحقق من العزل
3. ✅ توثيق كامل للعملية
4. ✅ إنشاء قوالب ملفات البيئة

---

## 🔴 الخطوات المطلوبة منك الآن

### الخطوة 1: إنشاء ملف .env.local محلي

**⚠️ هذا الملف محمي ولا يمكنني إنشاؤه، يجب عليك إنشاؤه يدوياً**

1. أنشئ ملف جديد في جذر المشروع اسمه `.env.local`
2. انسخ المحتوى من `env.example`
3. **غيّر السطر التالي فقط:**

```env
# قبل (PRODUCTION - خطر!):
DATABASE_URL="postgresql://neondb_owner:npg_qNFE0IHpk1vT@ep-dry-glitter-aftvvy9d-pooler.c-2.us-west-2.aws.neon.tech/neondb?sslmode=require&channel_binding=require"

# بعد (LOCAL - آمن):
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/speedy_van_dev"
```

4. احفظ الملف

---

### الخطوة 2: التحقق من العزل

```powershell
pnpm tsx scripts/verify-database-isolation.ts
```

**يجب أن ترى**: ✅ All Checks Passed

**إذا رأيت** 🚨 CRITICAL ALERT، توقف وراجع الخطوة 1.

---

### الخطوة 3: إعداد قاعدة البيانات المحلية

#### الخيار أ: الإعداد الآلي (موصى به)

```powershell
pnpm tsx scripts/setup-local-database.ts
```

هذا السكريبت سيقوم بـ:
- إنشاء قاعدة البيانات المحلية `speedy_van_dev`
- تطبيق جميع الترحيلات
- توليد Prisma Client
- التحقق من العزل

#### الخيار ب: الإعداد اليدوي

إذا فشل السكريبت:

```powershell
# 1. تأكد من تشغيل PostgreSQL
Get-Service postgresql*

# 2. إنشاء قاعدة البيانات
psql -U postgres -c "CREATE DATABASE speedy_van_dev;"

# 3. تطبيق الترحيلات
cd packages/shared
pnpm prisma migrate deploy

# 4. توليد Prisma Client
pnpm prisma generate

# 5. العودة للجذر
cd ../..
```

---

### الخطوة 4: التحقق النهائي

```powershell
# تحقق من الاتصال
pnpm tsx scripts/verify-database-isolation.ts

# تحقق من الجداول
psql -U postgres -d speedy_van_dev -c "\dt"
```

---

### الخطوة 5: تشغيل السيرفر بأمان

```powershell
pnpm dev
```

افتح المتصفح: http://localhost:3000

---

## 🔒 Security Checklist

تحقق من كل النقاط التالية قبل المتابعة:

- [ ] ملف `.env.local` موجود ويحتوي على `localhost` في DATABASE_URL
- [ ] سكريبت `verify-database-isolation.ts` يمر بنجاح ✅
- [ ] PostgreSQL يعمل محلياً
- [ ] قاعدة البيانات `speedy_van_dev` موجودة
- [ ] الترحيلات مطبقة (لا توجد أخطاء)
- [ ] Prisma Client مُولّد
- [ ] NODE_ENV=development في `.env.local`
- [ ] DISABLE_SMS=true في `.env.local`

---

## ❌ ما لا يجب فعله

- ❌ **أبداً** استخدم رابط قاعدة بيانات الإنتاج في `.env.local`
- ❌ **أبداً** تشغّل `prisma migrate` على قاعدة بيانات الإنتاج محلياً
- ❌ **أبداً** تختبر على بيانات حقيقية للعملاء
- ❌ **أبداً** تشغّل السيرفر قبل التحقق من العزل

---

## 🆘 إذا واجهت مشكلة

### PostgreSQL غير مثبت

```powershell
# تثبيت باستخدام Chocolatey
choco install postgresql

# أو حمّل من:
# https://www.postgresql.org/download/windows/
```

### خطأ في الاتصال بـ PostgreSQL

```powershell
# أعد تشغيل PostgreSQL
net stop postgresql-x64-15
net start postgresql-x64-15

# تحقق من الحالة
Get-Service postgresql*
```

### خطأ في كلمة المرور

```powershell
psql -U postgres
# إذا طُلبت كلمة المرور، استخدم "postgres"
# أو أعد تعيينها:
ALTER USER postgres PASSWORD 'postgres';
```

### الترحيلات تفشل

```powershell
# تحقق من حالة الترحيلات
cd packages/shared
pnpm prisma migrate status

# إعادة تطبيق الترحيلات
pnpm prisma migrate reset
pnpm prisma migrate deploy
```

---

## 📚 ملفات التوثيق

- **تفاصيل كاملة**: `DATABASE_SETUP_GUIDE.md`
- **بدء سريع**: `QUICK_START_DATABASE.md`
- **قالب البيئة**: انسخ من `env.example`

---

## 📞 الخلاصة

1. أنشئ `.env.local` مع DATABASE_URL محلي
2. شغّل `pnpm tsx scripts/verify-database-isolation.ts`
3. شغّل `pnpm tsx scripts/setup-local-database.ts`
4. تحقق من النجاح ✅
5. شغّل `pnpm dev`

**بعد اتباع هذه الخطوات، ستكون بيئة التطوير معزولة تماماً عن الإنتاج.**

---

**آخر تحديث**: 2025-01-16
**الأولوية**: 🚨 حرجة - يجب تنفيذها فوراً

